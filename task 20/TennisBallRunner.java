class TennisBallRunner
{
	public static void main(String... args)
	{
		TennisBall ball = new TennisBall();

		ball.bounce();
		ball.roll();
		ball.hit();
		ball.spin();
		ball.inflate();
		ball.deflate();
		ball.changeColor();
		ball.checkPressure();
		ball.measureWeight();
		ball.displayBrand();
		ball.pack();
		ball.unpack();
		ball.testQuality();
		ball.recycle();
		ball.store();
	}
}